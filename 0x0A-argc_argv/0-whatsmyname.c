#include <stdio.h>
/**
 * main - entry point of the c file
 * @argc: number of arguments to the program
 * @argv: array of arguments
 * Return: Always 0 (on success)
 */
int main (int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
